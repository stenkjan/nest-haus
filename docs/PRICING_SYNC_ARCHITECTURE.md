# Pricing Sync Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         GOOGLE SHEETS (Master Source)                    │
│                                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │ Nest_Groesse │  │Gebaeudehuelle│  │   Fenster    │  │ Belichtung │ │
│  │              │  │              │  │              │  │            │ │
│  │ Size | Price │  │ Mat | Mod.  │  │ Opt. | Price │  │ Pkg | Pr. │ │
│  │ ─────┼────── │  │ ────┼────── │  │ ─────┼────── │  │ ────┼──── │ │
│  │ Klein│ 15000 │  │ Holz│  +0   │  │ Std  │ 5000  │  │ Bas.│3000 │ │
│  │ Mitt.│ 22000 │  │ Alu │ +2000 │  │ Prem.│ 8500  │  │ Pre.│6000 │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └────────────┘ │
│                                                                          │
│  📊 You edit prices here → Everything syncs automatically              │
└────────────────────────┬─────────────────────────────────────────────────┘
                         │
                         │ Google Sheets API (Read-only)
                         ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      PRICING SYNC SERVICE                                │
│                   /src/services/pricing-sync.ts                          │
│                                                                          │
│  🔄 Sync Triggers:                                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────────┐ │
│  │   Manual     │  │   Scheduled  │  │      On Deployment           │ │
│  │ Admin Button │  │  Daily 2 AM  │  │   (Optional Future)          │ │
│  └──────┬───────┘  └──────┬───────┘  └──────────────────────────────┘ │
│         │                 │                                             │
│         └─────────┬───────┘                                             │
│                   ▼                                                     │
│  📥 Fetch & Parse:                                                      │
│  1. Read all sheets from Google Sheets                                 │
│  2. Parse each row based on sheet mapping                              │
│  3. Validate data & convert prices to cents                            │
│                   │                                                     │
│                   ▼                                                     │
│  🔍 Compare & Detect Changes:                                          │
│  - Get existing pricing from database                                  │
│  - Compare: basePrice, priceModifier, metadata                         │
│  - Classify: Added | Updated | Unchanged | Removed                     │
│                   │                                                     │
│                   ▼                                                     │
│  💾 Update Database:                                                   │
│  - INSERT new items                                                    │
│  - UPDATE changed prices (increment version)                           │
│  - MARK removed items as inactive                                      │
│  - LOG all changes with timestamp                                      │
└────────────────────────┬────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      DATABASE (PostgreSQL/Neon)                          │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                      pricing_data                                   │ │
│  ├────────────┬──────────┬──────────┬──────────┬──────────┬──────────┤ │
│  │ category   │ itemKey  │ itemName │basePrice │ version  │lastSynced│ │
│  ├────────────┼──────────┼──────────┼──────────┼──────────┼──────────┤ │
│  │nest_groesse│ klein    │ Klein    │ 1500000  │    3     │2025-11-06│ │
│  │nest_groesse│ mittel   │ Mittel   │ 2200000  │    1     │2025-11-06│ │
│  │gebaeude... │holz_klein│Holz Klein│       0  │    2     │2025-11-06│ │
│  │fenster     │ standard │ Standard │  500000  │    1     │2025-11-06│ │
│  └────────────┴──────────┴──────────┴──────────┴──────────┴──────────┘ │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                    pricing_sync_logs                                │ │
│  ├──────────────┬──────────────┬─────────┬────────────────────────────┤ │
│  │  syncedAt    │itemsUpdated  │ status  │         changes            │ │
│  ├──────────────┼──────────────┼─────────┼────────────────────────────┤ │
│  │2025-11-06 02:│      15      │ success │[{category: "nest_groesse", │ │
│  │    00:00     │              │         │  action: "updated",...}]   │ │
│  └──────────────┴──────────────┴─────────┴────────────────────────────┘ │
│                                                                          │
│  📊 Fast lookups, version history, complete audit trail                │
└────────────────────────┬────────────────────────────────────────────────┘
                         │
                         │ Prisma Client Queries
                         ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     NEST-HAUS KONFIGURATOR                               │
│                                                                          │
│  🏠 User selects:                                                       │
│  ┌───────────────┐  ┌────────────────┐  ┌──────────────┐              │
│  │ Nest: Klein   │→ │Material: Holz  │→ │Fenster: Std. │              │
│  └───────┬───────┘  └────────┬───────┘  └──────┬───────┘              │
│          │                   │                  │                       │
│          └───────────┬───────┴──────────────────┘                       │
│                      ▼                                                  │
│  💰 Price Calculator (Client-side):                                    │
│  const kleinPrice = await getPricing('nest_groesse', 'klein');         │
│  const holzModifier = await getPricing('gebaeudehuelle', 'holz_klein');│
│  const fensterPrice = await getPricing('fenster', 'standard');         │
│                      │                                                  │
│  Total = kleinPrice + holzModifier + fensterPrice                      │
│        = 15,000 + 0 + 5,000 = €20,000                                  │
│                      │                                                  │
│                      ▼                                                  │
│  ✅ Display to user: "Ihr Nest: €20.000"                               │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## API Flow Diagram

```
┌──────────────────────────────────────────────────────────────────────┐
│                          API ENDPOINTS                                │
└──────────────────────────────────────────────────────────────────────┘

Manual Sync (Admin Dashboard):
┌─────────────┐
│   Admin     │
│  Dashboard  │
└──────┬──────┘
       │ Click "Sync Now"
       │ + Enter admin password
       ▼
POST /api/sync/pricing
    ├─ Verify admin password
    ├─ Call PricingSyncService.syncPricing()
    ├─ Return results with timing
    └─ Response:
       {
         "success": true,
         "itemsAdded": 5,
         "itemsUpdated": 3,
         "duration": 2345ms
       }

Scheduled Sync (Automatic):
┌─────────────┐
│   Vercel    │
│    Cron     │
│  (Daily 2AM)│
└──────┬──────┘
       │ Trigger: 0 2 * * *
       │ Auth: Bearer [CRON_SECRET]
       ▼
GET /api/cron/sync-pricing
    ├─ Verify Vercel cron secret
    ├─ Call PricingSyncService.syncPricing()
    ├─ Send notification if changes > 5
    └─ Log to Vercel dashboard

Get Status:
┌─────────────┐
│   Admin     │
│  Dashboard  │
└──────┬──────┘
       │ On page load
       ▼
GET /api/sync/pricing
    ├─ Query pricing_sync_logs (last sync)
    ├─ Calculate next scheduled sync
    └─ Response:
       {
         "lastSync": "2025-11-06T12:00:00Z",
         "nextScheduledSync": "2025-11-07T02:00:00Z"
       }
```

---

## Data Flow: Excel → Google Sheets → Database → Configurator

```
Step 1: Excel Migration (One-time)
┌────────────────────┐
│  Excel File.xlsx   │
│  (Microsoft Excel) │
│  - Multiple sheets │
│  - Formulas        │
│  - Pricing data    │
└─────────┬──────────┘
          │ Upload to Google Drive
          │ Open with Google Sheets
          ▼
┌────────────────────┐
│  Google Sheets     │ ✅ Formulas preserved (~95%)
│  (Online)          │ ✅ Multiple tabs intact
│  - Collaborative   │ ✅ Live updates
│  - Accessible API  │ ✅ Version history
└────────────────────┘

Step 2: Sync Process (Daily/Manual)
┌────────────────────┐
│  Google Sheets     │
│  You edit prices   │
└─────────┬──────────┘
          │ Read via API
          ▼
┌────────────────────┐
│ Pricing Sync       │
│ Service            │
│ - Fetch            │ Changes detected:
│ - Parse            │ • Klein: €15k → €16k ✓
│ - Compare          │ • Mittel: €22k (unchanged)
│ - Update           │ • Groß: New item +
└─────────┬──────────┘
          │ Write to DB
          ▼
┌────────────────────┐
│  Database          │
│  pricing_data      │ ✅ Updated instantly
│  pricing_sync_logs │ ✅ All changes logged
└────────────────────┘

Step 3: Configurator Usage (Real-time)
┌────────────────────┐
│  User on website   │
│  Selects options   │
└─────────┬──────────┘
          │ Query database
          ▼
┌────────────────────┐
│  Konfigurator      │
│  Price Calculator  │ Reads from DB cache
└─────────┬──────────┘
          │ Display price
          ▼
┌────────────────────┐
│  User sees:        │
│  "Ihr Nest: €16k"  │ ✅ Up-to-date price
└────────────────────┘
```

---

## Environment Variables Flow

```
Development (.env.local):
┌────────────────────────────────────────────────────────┐
│ PRICING_SPREADSHEET_ID="1A2B3C4..."                    │
│ GOOGLE_SERVICE_ACCOUNT_EMAIL="service@project.iam..." │
│ GOOGLE_SERVICE_ACCOUNT_KEY="-----BEGIN PRIVATE..."    │
│ GOOGLE_SERVICE_ACCOUNT_KEY_FILE="service-account.json"│ ← Alternative
│ ADMIN_PASSWORD="2508DNH-d-w-i-d-z"                     │
│ CRON_SECRET="random-secret-for-cron"                   │
└────────────────────────────────────────────────────────┘
                         │
                         ▼
           Next.js App (localhost:3000)
                         │
           ┌─────────────┴─────────────┐
           ▼                           ▼
    Manual Sync API          Scheduled Cron
    (admin dashboard)        (not active locally)


Production (Vercel):
┌────────────────────────────────────────────────────────┐
│ Vercel Dashboard → Settings → Environment Variables    │
│                                                         │
│ PRICING_SPREADSHEET_ID     = "1A2B3C4..."              │
│ GOOGLE_SERVICE_ACCOUNT_EMAIL = "service@..."           │
│ GOOGLE_SERVICE_ACCOUNT_KEY = "-----BEGIN..."           │
│ ADMIN_PASSWORD             = "2508DNH-d-w-i-d-z"       │
│ CRON_SECRET                = "vercel-auto-generated"   │
└────────────────────────────────────────────────────────┘
                         │
                         ▼
           Vercel Deployment (nest-haus.at)
                         │
           ┌─────────────┴─────────────┐
           ▼                           ▼
    Manual Sync API          Scheduled Cron ⏰
    (admin dashboard)        (triggers at 2 AM)
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│              GOOGLE SHEETS (Data Source)                     │
│  🔒 Service Account: READ-ONLY access                       │
│  📧 Email: service@project-id.iam.gserviceaccount.com       │
│  ✅ Cannot modify sheets, only read                         │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              SYNC SERVICE (Server-side)                      │
│  🔐 Credentials from environment variables                  │
│  ⚠️  Private key never exposed to client                    │
└──────────────────────┬──────────────────────────────────────┘
                       │
         ┌─────────────┴─────────────┐
         ▼                           ▼
┌─────────────────────┐    ┌──────────────────────┐
│   Manual Sync API   │    │  Scheduled Cron API  │
│ POST /api/sync/...  │    │ GET /api/cron/...    │
│                     │    │                      │
│ 🔒 Requires:        │    │ 🔒 Requires:         │
│ - adminPassword     │    │ - Vercel cron secret │
│ - Must match env    │    │ - Bearer token       │
└─────────────────────┘    └──────────────────────┘
         │                           │
         └─────────────┬─────────────┘
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                   DATABASE (Postgres)                        │
│  🔒 Connection string in environment                        │
│  📝 All changes logged with timestamp                       │
│  📊 Version tracking for audit                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Sheet Mapping Reference

```
Google Sheet Structure → Database Mapping

Sheet: "Nest_Groesse"
┌──────────┬───────────┬────────────┬───────────┐
│ A: Size  │ B: Width  │ C: Height  │ D: Price  │
├──────────┼───────────┼────────────┼───────────┤
│ Klein    │ 2.5       │ 2.5        │ 15000     │
└──────────┴───────────┴────────────┴───────────┘
                      ↓
Database: pricing_data
┌──────────────┬──────────┬───────────┬───────────┬──────────────────────┐
│ category     │ itemKey  │ itemName  │ basePrice │ metadata             │
├──────────────┼──────────┼───────────┼───────────┼──────────────────────┤
│nest_groesse  │ klein    │ Klein     │ 1500000   │{width:2.5,height:2.5}│
└──────────────┴──────────┴───────────┴───────────┴──────────────────────┘
                                                     ↑
                                            Stored in cents


Sheet: "Gebaeudehuelle"
┌──────────┬──────────────┬────────────────┬──────────┐
│ A: Size  │ B: Material  │ C: Modifier    │ D: Notes │
├──────────┼──────────────┼────────────────┼──────────┤
│ Klein    │ Holz         │ 0              │          │
│ Klein    │ Aluminium    │ +2000          │          │
└──────────┴──────────────┴────────────────┴──────────┘
                      ↓
Database: pricing_data
┌──────────────┬───────────────┬────────────────┬───────────┬───────────────┐
│ category     │ itemKey       │ itemName       │ basePrice │priceModifier  │
├──────────────┼───────────────┼────────────────┼───────────┼───────────────┤
│gebaeudehuelle│klein_holz     │Klein - Holz    │     0     │      0        │
│gebaeudehuelle│klein_aluminium│Klein - Alu     │     0     │  200000       │
└──────────────┴───────────────┴────────────────┴───────────┴───────────────┘
```

---

## Complete System Lifecycle

```
📅 Day 0: Setup
1. Share Google Sheet with service account ✓
2. Configure environment variables ✓
3. Run initial sync (manually) ✓
4. Verify data in database ✓
5. Deploy to production ✓

📅 Day 1-N: Daily Operations
02:00 AM → Vercel Cron triggers automatic sync
         → Fetches from Google Sheets
         → Compares with database
         → Updates only changes
         → Logs result

09:00 AM → Admin checks sync log
         → Views changes in dashboard
         → Verifies prices in configurator

10:00 AM → Marketing updates Google Sheet
         → Changes a price: €15,000 → €16,000
         → Adds a new product option

10:05 AM → Admin triggers manual sync
         → Sees: "1 updated, 1 added"
         → New price live immediately

12:00 PM → Customer visits website
         → Uses configurator
         → Sees latest prices
         → Makes inquiry

📅 Ongoing
- Daily automatic syncs at 2 AM ✓
- Manual syncs when needed ✓
- Complete audit trail ✓
- Version tracking ✓
- Error notifications ✓
```

---

## File Structure

```
/workspace
├── src/
│   ├── services/
│   │   └── pricing-sync.ts           ← Core sync logic
│   ├── app/
│   │   └── api/
│   │       ├── sync/
│   │       │   └── pricing/
│   │       │       └── route.ts      ← Manual sync endpoint
│   │       └── cron/
│   │           └── sync-pricing/
│   │               └── route.ts      ← Scheduled sync endpoint
│   └── components/
│       └── admin/
│           └── PricingSyncPanel.tsx  ← Admin UI component
├── prisma/
│   └── schema.prisma                 ← Database schema (updated)
├── docs/
│   ├── PRICING_SYNC_SETUP.md         ← Complete setup guide
│   ├── PRICING_SYNC_ENV_VARS.md      ← Environment variables
│   ├── PRICING_SYNC_QUICK_REF.md     ← Quick reference
│   └── PRICING_SYNC_IMPLEMENTATION_COMPLETE.md ← This summary
└── vercel.json                       ← Cron configuration (updated)
```

---

**Summary**: Your pricing now flows from Google Sheets → Database → Configurator automatically, with complete control, audit trail, and error handling! 🎉
